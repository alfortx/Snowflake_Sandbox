Terraform の実行計画を確認してください。

## 前提確認

1. `.env` ファイルが存在するか確認する
   - 存在しない場合: `cp .env.example .env` を案内して中断する

## 実行

```
bash scripts/tf-plan.sh
```

- エラーがあれば内容を日本語で説明する
- 変更があれば、追加・変更・削除の件数とリソース名をサマリーとして報告する
