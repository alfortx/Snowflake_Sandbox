# =============================================================================
# アカウントレベルのパラメータ設定
# ACCOUNTADMIN: アカウント全体に影響するパラメータを管理
# =============================================================================

# -----------------------------------------------------------------------------
# Cortex クロスリージョン推論の有効化
#
# 設定値:
#   - ANY_REGION: すべての対応リージョンで推論処理を許可（最も柔軟）
#   - AWS_US: 米国AWSリージョン（us-east-1, us-west-2）のみ
#   - AWS_EU: 欧州AWSリージョン（eu-central-1）のみ
#   - AWS_APJ: アジア太平洋AWSリージョン（ap-northeast-1）のみ
#   - コンマ区切り: 複数地域の組み合わせ（例: "AWS_US,AWS_EU"）
#
# 注意事項:
#   - デフォルト（DISABLED）では、デフォルトリージョンでのみ推論処理が実行されます
#   - クレジットは要求元リージョンで消費されます
#   - データ転送料金（egress）は発生しません
#   - アカウント全体に影響するため、全ユーザー・全ロールに適用されます
# -----------------------------------------------------------------------------
resource "snowflake_current_account" "account" {
  provider = snowflake.accountadmin

  cortex_enabled_cross_region = "ANY_REGION"
}
